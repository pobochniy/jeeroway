# HTTPS Setup для JeerowayWiki.Images (без nginx)

Простая настройка HTTPS для C# приложения, работающего напрямую в Docker.

## Архитектура

```
Internet → [VM IP/Domain] → Docker Container (ASP.NET Core с Kestrel)
                              ├─ Port 80 (HTTP) → redirect to HTTPS
                              └─ Port 443 (HTTPS) ✓
```

## Предварительные требования

1. ✅ Виртуальная машина с Ubuntu/Debian
2. ✅ Docker установлен
3. ✅ Домен настроен и указывает на IP виртуальной машины
4. ✅ Порты 80 и 443 открыты в файрволе

## Быстрая установка

### Шаг 1: На виртуальной машине

Скопируйте скрипт `setup-ssl.sh` на VM и запустите:

```bash
# Скопируйте скрипт на VM
scp setup-ssl.sh user@your-vm-ip:~/

# Подключитесь к VM
ssh user@your-vm-ip

# Запустите установку
chmod +x setup-ssl.sh
./setup-ssl.sh your-domain.com your-email@example.com
```

**Пример:**
```bash
./setup-ssl.sh img.jeeroway.ru admin@jeeroway.ru
```

Скрипт автоматически:
- Установит Certbot
- Получит SSL сертификат от Let's Encrypt
- Конвертирует сертификат в формат PFX для ASP.NET Core
- Настроит автоматическое обновление сертификата

### Шаг 2: Добавьте GitHub Secret

В настройках вашего GitHub репозитория добавьте секрет:

- **Name:** `DOMAIN_NAME`
- **Value:** `your-domain.com` (без www)

**Путь:** Repository → Settings → Secrets and variables → Actions → New repository secret

### Шаг 3: Запустите GitHub Actions

Запустите workflow `Publish Images service` вручную:

1. GitHub → Actions → Publish Images service
2. Run workflow → Run workflow

Workflow автоматически:
- Соберет приложение
- Создаст Docker образ
- Запустит контейнер с монтированием SSL сертификата

## Проверка работы

### 1. Проверьте HTTP → HTTPS редирект

```bash
curl -I http://your-domain.com
```

Ожидается: `307 Temporary Redirect` или `301 Moved Permanently`

### 2. Проверьте HTTPS

```bash
curl -I https://your-domain.com
```

Ожидается: `200 OK`

### 3. Проверьте сертификат

```bash
openssl s_client -connect your-domain.com:443 -servername your-domain.com
```

Или откройте в браузере и проверьте замок 🔒

### 4. Проверьте контейнер

```bash
docker ps | grep jimgs
docker logs jimgs
```

## Что изменилось в коде

### 1. `appsettings.json`

Добавлена конфигурация Kestrel для HTTPS:

```json
{
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:8080"
      },
      "Https": {
        "Url": "https://0.0.0.0:8443",
        "Certificate": {
          "Path": "/app/certs/certificate.pfx",
          "Password": ""
        }
      }
    }
  }
}
```

### 2. `Startup.cs`

Добавлен HTTPS редирект только для production:

```csharp
if (!env.IsDevelopment())
{
    app.UseHttpsRedirection();
}
```

### 3. GitHub Actions Workflow

Обновлен запуск контейнера:

```yaml
docker run 
  --restart unless-stopped 
  -p 80:8080      # HTTP
  -p 443:8443     # HTTPS
  -v /etc/letsencrypt/live/${{ secrets.DOMAIN_NAME }}:/app/certs:ro
  ...
```

## Автоматическое обновление сертификата

Скрипт `setup-ssl.sh` автоматически настраивает cron job:

```bash
# Проверяется ежедневно в 3:00
0 3 * * * /usr/local/bin/renew-cert-aspnet.sh
```

Сертификат обновляется за 30 дней до истечения.

### Ручное обновление

Если нужно обновить сертификат вручную:

```bash
sudo /usr/local/bin/renew-cert-aspnet.sh
```

## Структура файлов на VM

```
/etc/letsencrypt/
└── live/
    └── your-domain.com/
        ├── fullchain.pem       # Публичный сертификат
        ├── privkey.pem         # Приватный ключ
        └── certificate.pfx     # Сертификат для ASP.NET Core
```

## Endpoints

После настройки приложение доступно по:

- `http://your-domain.com` → автоматически редиректит на HTTPS
- `https://your-domain.com` ✓

## Troubleshooting

### Ошибка: "Unable to configure HTTPS endpoint"

**Причина:** Сертификат не найден или неправильные права доступа.

**Решение:**
```bash
# Проверьте наличие сертификата
ls -la /etc/letsencrypt/live/your-domain.com/certificate.pfx

# Установите правильные права
sudo chmod 644 /etc/letsencrypt/live/your-domain.com/certificate.pfx

# Перезапустите контейнер
docker restart jimgs
```

### Ошибка: "Port 80 already in use"

**Причина:** Другой сервис использует порт 80.

**Решение:**
```bash
# Найдите процесс
sudo lsof -i :80

# Остановите контейнер если нужно
docker stop jimgs

# Запустите setup-ssl.sh снова
./setup-ssl.sh your-domain.com your-email@example.com
```

### Ошибка: "Failed to verify certificate"

**Причина:** Домен не указывает на IP виртуальной машины.

**Решение:**
```bash
# Проверьте DNS
nslookup your-domain.com

# Должен вернуть IP вашей VM
```

### Контейнер не стартует

**Проверьте логи:**
```bash
docker logs jimgs

# Если ошибка с сертификатом, проверьте монтирование
docker inspect jimgs | grep -A 10 Mounts
```

## Безопасность

Настроенная конфигурация включает:

- ✅ **TLS 1.2 и 1.3** - современные протоколы
- ✅ **HTTPS Redirection** - автоматический редирект с HTTP
- ✅ **Let's Encrypt** - бесплатный доверенный сертификат
- ✅ **Auto-renewal** - автоматическое обновление
- ✅ **Read-only mount** - сертификаты монтируются только для чтения

## Дополнительные настройки (опционально)

### Добавить HSTS

В `Startup.cs` добавьте:

```csharp
app.UseHsts();
```

### Настроить минимальную версию TLS

В `appsettings.json`:

```json
{
  "Kestrel": {
    "Endpoints": {
      "Https": {
        "SslProtocols": ["Tls12", "Tls13"]
      }
    }
  }
}
```

## Полезные команды

```bash
# Проверить статус сертификата
sudo certbot certificates

# Проверить срок действия
openssl x509 -in /etc/letsencrypt/live/your-domain.com/fullchain.pem -noout -dates

# Тест обновления (без фактического обновления)
sudo certbot renew --dry-run

# Посмотреть логи контейнера
docker logs -f jimgs

# Перезапустить контейнер
docker restart jimgs

# Проверить открытые порты
sudo netstat -tulpn | grep -E ':(80|443)'
```

## Отличия от nginx решения

| Аспект | С nginx | Без nginx (текущее) |
|--------|---------|---------------------|
| Сложность | Выше | Ниже |
| Компонентов | 3 (nginx + certbot + app) | 2 (certbot + app) |
| Производительность | Выше для статики | Достаточно для API |
| Гибкость | Больше возможностей | Проще настройка |
| Обслуживание | Больше конфигов | Меньше файлов |

Для простого API текущее решение оптимально. Если в будущем понадобится:
- Балансировка нагрузки
- Кеширование
- Несколько приложений
- Сложная маршрутизация

Тогда можно добавить nginx как reverse proxy.

## Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker logs jimgs`
2. Проверьте сертификат: `sudo certbot certificates`
3. Проверьте DNS: `nslookup your-domain.com`
4. Проверьте файрволл: `sudo ufw status`

---

**Готово!** Ваше приложение теперь работает по HTTPS с автоматическим обновлением сертификата. 🎉
